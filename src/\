
# builtin ret
@noinline function _return(a)
    a
end

function forloop(body, iter, captures...)
    # body is assumed to be a function that takes an iteration variable
    # and an array of captured values from the previous iteration or, in the first step, the outer scope.
    # the returned l takes an iterator and a list of those values captured from outer scope, and loops.
    for i in iter
        captures = body(i, captures)
    end
    return captures
end

# builtin gauss
#@noinline function gauss(s, ϵ, δ, f)
#    return (x) -> f(x) + rand(Normal(0, 2*s^2*(log(1.25/δ))/ϵ^2), 1)[1]
#end

